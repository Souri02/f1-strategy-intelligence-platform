# Serverless (Vercel): metadata + Parquet reads only — no sklearn/xgboost (stays under 500 MB).
# Full backend: requirements.full.txt
fastapi
pydantic
pydantic-settings
pandas
numpy
requests
pyarrow
python-dotenv
