# Vercel installs this file by default — keep it slim (under serverless bundle limits).
# Full local/Docker stack (FastF1, uvicorn extras): pip install -r requirements.full.txt
-r requirements.vercel.txt
