# Slim install for Vercel (flat list only — no `-r` includes; Vercel’s parser does not support them).
# Full local/Docker: pip install -r requirements.full.txt
fastapi
pydantic
pydantic-settings
pandas
numpy
requests
pyarrow
python-dotenv
scikit-learn
xgboost
joblib
