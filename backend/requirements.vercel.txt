# Slim deps for Vercel Python (500 MB Lambda bundle limit).
# Full stack with FastF1 + telemetry: use backend/requirements.full.txt (Docker/local).
# Omit uvicorn — Vercel invokes the ASGI app directly.
fastapi
pydantic
pydantic-settings
pandas
numpy
requests
pyarrow
python-dotenv
scikit-learn
xgboost
joblib
