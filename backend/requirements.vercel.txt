# Kept in sync with `requirements.txt` (same flat list).
# Vercel reads `requirements.txt` only — do not use `-r` includes there.
# Full stack: `requirements.full.txt` (Docker/local).
fastapi
pydantic
pydantic-settings
pandas
numpy
requests
pyarrow
python-dotenv
scikit-learn
xgboost
joblib
